"""
The logic, with no window attached.

Everything here works against the live workbook over COM and returns
plain data. Nothing in this package imports from `ui`, so the same code
runs from a script, a test, or the form.
"""
